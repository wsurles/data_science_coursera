m <- ceiling(sqrt(n <- length(cl <- colors())))
length(cl) <- m*m; cm <- matrix(cl, m)
##
require("graphics")
op <- par(mar=rep(0,4), ann=FALSE, bg = bg); on.exit(par(op))
plot(1:m,1:m, type="n", axes=FALSE)
text(col(cm), rev(row(cm)), cm,  col = cl, cex=cex, srt=srt)
}
showCols1()
showCols1()
showCols1 <- function(bg = "gray", cex = 0.75, srt = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
##
require("graphics")
op <- par(mar=rep(0,4), ann=FALSE, bg = bg); on.exit(par(op))
plot(1:m,1:m, type="n", axes=FALSE)
text(col(cm), rev(row(cm)), cm,  col = cl, cex=cex, srt=srt, fontface = 'bold')
}
showCols1(my_colors = c1)
?text
showCols2 <- function(bg = "grey", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .92, h = .92)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
showCols2(my_colors = c2)
grid.newpage(); vp <- viewport(w = .9, h = .9)
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
bg = "grey20"
grid.rect(gp=gpar(fill=bg))
bg = "grey70"
grid.rect(gp=gpar(fill=bg))
bg = "grey80", cex = 1, rot = 30, my_colors) {
grid.rect(gp=gpar(fill=bg))
bg = "grey80"
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
c2 <- c[grep('grey',c)]
c2 <- c[grep('orange',c)]
c3 <- c[grep('grey',c)]
showCols2(my_colors = c3)
cpal <- colorRampPalette(c("springgreen4", "gray90", "orangered"))
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("springgreen4", "gray85", "orangered"))
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("springgreen4", "gray85", "orangered"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
## show color options with 'grid' package:
showCols2 <- function(bg = "grey80", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
require(graphics)
colors()
num <- 15
c <- gray(0:num/num)
pie(rep(1,length(c)), col=c)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
pie(rep(1,length(cpal)), col=cpal)
cpal
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
colorRamp(c("red","green","blue"))( (0:num)/num ) ## (x) , x in [0,1]
cpal <- colorRampPalette(c("blue","red"))( 8 ) ## (n)
cpal
pie(rep(1,length(cpal)), col <- cpal)
pie(rep(1,length(cpal)), col <- cpal)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
pie(rep(1,length(cpal)), col <- cpal)
cpal
cpal <- colorRampPalette(c("steelblue", "tomato4"), space = "Lab")
pie(rep(1,length(cpal)), col <- cpal)
cpal <- colorRampPalette(c("steelblue", "tomato4"))(8)
cpal
pie(rep(1,length(cpal)), col <- cpal)
cpal <- colorRampPalette(c("steelblue", "gray", "tomato4"))(8)
pie(rep(1,length(cpal)), col <- cpal)
colors()
c1 <- c[grep('green',c)]
showCols2(my_colors = c3)
showCols2 <- function(bg = "grey80", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
colors()
c1 <- c[grep('green',c)]
c3 <- c[grep('grey',c)]
showCols2(my_colors = c3)
c3 <- c[grep('grey',c)]
c3
c <- colors()
c3 <- c[grep('grey',c)]
showCols2(my_colors = c3)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("springgreen4", "gray85", "orangered"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
?colorRamp
YlOrBr <- c("#FFFFD4", "#FED98E", "#FE9929", "#D95F0E", "#993404")
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab"),
asp = 1)
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab",
bias = 0.5),
asp = 1)
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab"),
asp = 1)
jet.colors <-
colorRampPalette(c("#00007F", "blue", "#007FFF", "cyan",
"#7FFF7F", "yellow", "#FF7F00", "red", "#7F0000"))
jet.colors
filled.contour(volcano, color = jet.colors, asp = 1)
m <- outer(1:20,1:20,function(x,y) sin(sqrt(x*y)/3))
rgb.palette <- colorRampPalette(c("red", "orange", "blue"),
space = "rgb")
Lab.palette <- colorRampPalette(c("red", "orange", "blue"),
space = "Lab")
filled.contour(m, col = rgb.palette(20))
filled.contour(m, col = Lab.palette(20))
filled.contour(m, col = rgb.palette(20))
filled.contour(m, col = Lab.palette(20))
filled.contour(m, col = rgb.palette(20))
filled.contour(m, col = Lab.palette(20))
colorRamp(c("red","green"))( (0:4)/4 )
(0:4)/4
colorRamp(c("red","green"))
colorRamp(c("red","green"))( (0:4)/4 )
c <- colorRamp(c("red","green"))( (0:4)/4 ) ## (x) , x in [0,1]
filled.contour(volcano, color.palette = c, asp = 1)
c <- colorRamp(c("red","green"))( (0:4)/4 ) ## (x) , x in [0,1]
c
filled.contour(volcano, color.palette = c, asp = 1)
YlOrBr <- c("#FFFFD4", "#FED98E", "#FE9929", "#D95F0E", "#993404")
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab"),
asp = 1)
colorRampPalette(YlOrBr, space = "Lab")
filled.contour(volcano, color.palette = colorRamp(c("red","green")), asp = 1)
c <- colorRamp(c("red","green"))
c
filled.contour(volcano, color.palette = c(0:1), asp = 1)
c
filled.contour(volcano,
color.palette = colorRamp(YlOrBr, space = "Lab"),
asp = 1)
filled.contour(volcano,
color.palette = colorRamp(YlOrBr),
asp = 1)
pal <- colorRamp(c(''red','blue''))
pal
pal <- colorRamp(c('red','blue'))
pal <- colorRamp(c("red","blue"))
pal <- colorRamp(c("red", "blue"))
pal <- colorRamp(c("red","blue"))
pal <- colorRamp(c("red"))
pal
pal(0)
pal(1)
pal(2)
pal(seq(0,1,len=10))
pal <- colorRamp(c("red","blue"))
?colorRamp
colorRamp(c("red","blue"), bias = 1)
pal <- colorRamp(c("red","green"), bias = 1)
pal <- colorRamp(c("red","green"))(0:1)
colorRamp(c("red","green"))( (0:4)/4 ) ## (x) , x in [0,1]
require(graphics)
require(grDevices)
pal <- colorRamp(c("red","green"))(0:1)
colorRamp(c("red","green"))( (0:4)/4 ) ## (x) , x in [0,1]
require(grDevices)
library(grDevices)
pal <- colorRamp(c("red","green"))(0:1)
require(grDevices)
pal <- colorRamp(c("red","green"))(0:1)
pal(0)
pal
pal <- colorRamp(c("red","green"))
pal(0)
pal(1)
pal(2)
pal <- colorRamp(c("red","blue"))
pal(.5)
pal <- colorRamp(c("red","blue"))
pal(seq(0,1,len=10))
pal(2)
pal <- colorRampPallette(c("red","blue"))
pal(2)
pal <- colorRampPalette(c("red","blue"))
pal(2)
pal(10)
library(RColorBrewer)
cols <- brewer.pal(3,"BuGn")
?RColorBrewer
brewer.pal.info
brewer.pal
plot(brewer.pal)
display.brewer.all
display.brewer.all()
display.brewer.all()
brewer.pal.info
?RColorBrewer
cols <- brewer.pal(3,"BuGn")
cols
pal <- colorRampPallette(cols)
pal <- colorRampPalette(cols)
image(volcano,col = pal(20))
volcano
filled.contour(volcano, col = pal(20))
filled.contour(volcano, col = pal(20), asp = 1)
display.brewer.all()
cols <- brewer.pal(3,"YIOrRd")
cols <- brewer.pal(3,"YlOrRd")
pal <- colorRampPalette(cols)
filled.contour(volcano, col = pal(20), asp = 1)
filled.contour(volcano, col = pal(11), asp = 1)
filled.contour(volcano, col = pal(11), asp = 2)
filled.contour(volcano, col = pal(11), asp = .5)
filled.contour(volcano, col = pal(11))
filled.contour(volcano, col = pal(22))
filled.contour(volcano, col = pal(21))
filled.contour(volcano, col = pal(20))
filled.contour(volcano, col = pal(3))
filled.contour(volcano, col = pal(21))
image(volcano,col = pal(20))
image(volcano,col = pal(5))
display.brewer.all()
cols <- brewer.pal(3,"Oranges")
pal <- colorRampPalette(cols)
image(volcano,col = pal(5))
filled.contour(volcano, col = pal(21))
pal(21)
display.brewer.all()
cols <- brewer.pal(3,"Spectral")
pal <- colorRampPalette(cols)
filled.contour(volcano, col = pal(21))
filled.contour(volcano, col = pal(21), space = "Lab")
pal <- colorRampPalette(cols, space = "Lab")
filled.contour(volcano, col = pal(21))
pal <- colorRampPalette(cols)
display.brewer.all()
cols <- brewer.pal(3,"RdBu")
cols <- brewer.pal(3,"RdBu")
# cols <- brewer.pal(3,"Spectral")
pal <- colorRampPalette(cols)
image(volcano,col = pal(5))
cols <- brewer.pal(3,"RdBu")
# cols <- brewer.pal(3,"Spectral")
pal <- colorRampPalette(cols)
image(volcano,col = pal(20))
display.brewer.all()
-cols <- brewer.pal(3,"RdBu")
brewer.pal(3,"RdBu")
pie(rep(1,length(cols)), col=cols)
cols <- brewer.pal(5,"RdBu")
pie(rep(1,length(cols)), col=cols)
display.brewer.all()
cols <- brewer.pal(5,"GnBu")
pie(rep(1,length(cols)), col=cols)
YlOrBr <- c("#FFFFD4", "#FED98E", "#FE9929", "#D95F0E", "#993404")
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab"),
asp = 1)
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr, space = "Lab",
bias = 0.5),
asp = 1)
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr),
asp = 1)
filled.contour(volcano,
color.palette = colorRampPalette(YlOrBr))
filled.contour(volcano, color.palette = colorRampPalette(YlOrBr, space = "Lab", bias = 0.5))
cpal <- colorRampPalette(c("springgreen4", "gray85", "orangered"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("gray85", "orangered"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("gray85", "orangered"), space = "Lab", bias = 1.5)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered"), space = "Lab", bias = 1.5)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), space = "Lab", bias = 1.5)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), space = "Lab", bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orange", "darkred"), space = "Lab", bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "gray50", "darkred"), space = "Lab", bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
showCols2 <- function(bg = "grey80", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
c2 <- c[grep('orange',c)]
showCols2 <- function(bg = "grey80", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
showCols2(my_colors = c3)
c3 <- c[grep('grey',c)]
showCols2(my_colors = c3)
c <- colors()
c3 <- c[grep('grey',c)]
c1 <- c[grep('gray',c)]
showCols2(my_colors = c1)
c1 <- c[grep('orange',c)]
showCols2(my_colors = c1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), space = "Lab", bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
?space
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "rgb")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
c1 <- c[grep('blue',c)]
showCols2(my_colors = c1)
cpal <- colorRampPalette(c("white", "steelblue", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
showCols2(my_colors = c1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal
filled.contour(volcano, color.palette = cpal(5), asp = 1)
cpal
filled.contour(volcano, color.palette = cpal(2), asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)(20)
cpal
filled.contour(volcano, color.palette = cpal, asp = 1)
color.palette
colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)(20)
filled.contour(volcano, color.palette = cpal, asp = 1)
filled.contour(volcano,  cpal, asp = 1)
?filled.contour
?filled.contour(volcano,  col = cpal, asp = 1)
filled.contour(volcano,  col = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)(21)
filled.contour(volcano,  col = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)(3)
filled.contour(volcano,  col = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
filled.contour(volcano, color.palette = cpal)
require(grDevices)
require(graphics)
pal <- colorRamp(c("red","blue"))
pal(0)
pal(1)
pal(.5)
pal(seq(0,1,len=10))
pal <- colorRampPalette(c("red","blue"))
pal
pal(2)
pal(10)
num <- 15
c <- gray(0:num/num)
pie(rep(1,length(c)), col=c)
library(RColorBrewer)
brewer.pal.info
display.brewer.all()
cols <- brewer.pal(5,"GnBu")
pie(rep(1,length(cols)), col=cols)
cols <- brewer.pal(10,"GnBu")
pie(rep(1,length(cols)), col=cols)
pal <- colorRampPalette(cols)
image(volcano,col = pal(20))
pal
filled.contour(volcano, col = pal(20))
filled.contour(volcano, col = pal(21))
showCols2 <- function(bg = "grey80", cex = 1, rot = 30, my_colors) {
m <- ceiling(sqrt(n <- length(cl <- my_colors)))
length(cl) <- m*m; cm <- matrix(cl, m)
require("grid")
grid.newpage(); vp <- viewport(w = .9, h = .9)
grid.rect(gp=gpar(fill=bg))
grid.text(cm, x = col(cm)/m, y = rev(row(cm))/m, rot = rot,
vp=vp, gp=gpar(cex = cex, col = cm, fontface = 'bold'))
}
c <- colors()
c1 <- c[grep('blue',c)]
showCols2(my_colors = c1)
cpal <- colorRampPalette(c("steelblue", "gray80", "tomato4"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("springgreen4", "gray85", "orangered"), space = "Lab")
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "orangered", "darkred"), bias = 1)
filled.contour(volcano, color.palette = cpal, asp = 1)
cpal <- colorRampPalette(c("white", "lightsteelblue3", "steelblue4"), bias = 1)
filled.contour(volcano, color.palette = cpal)
YlOrBr <- c("#FFFFD4", "#FED98E", "#FE9929", "#D95F0E", "#993404")
filled.contour(volcano, color.palette = colorRampPalette(YlOrBr))
filled.contour(volcano, color.palette = colorRampPalette(YlOrBr, space = "Lab", bias = 0.5))
jet.colors <-
colorRampPalette(c("#00007F", "blue", "#007FFF", "cyan",
"#7FFF7F", "yellow", "#FF7F00", "red", "#7F0000"))
filled.contour(volcano, color = jet.colors, asp = 1)
m <- outer(1:20,1:20,function(x,y) sin(sqrt(x*y)/3))
rgb.palette <- colorRampPalette(c("red", "orange", "blue"),
rgb.palette <- colorRampPalette(c("red", "orange", "blue"),
space = "rgb")
rgb.palette <- colorRampPalette(c("red", "orange", "blue"), space = "rgb")
Lab.palette <- colorRampPalette(c("red", "orange", "blue"), space = "Lab")
x <- rnorm(10000)
y <- rnorm(10000)
smoothScatter(x,y)
?rgb
?rgb
rgb(200,200,0,1)
rgb(.5,.5,0,1)
?colorspace
plot(xy,pch = 19)
plot(x,y,pch = 19)
plot(x,y,col = rgb(.5,.5,0,.5 ), pch = 19)
plot(x,y,col = rgb(0,0,.5,.5), pch = 19)
plot(x,y,col = rgb(1,1,.5,.5), pch = 19)
plot(x,y,col = rgb(.2,.2,1,.5), pch = 19)
plot(x,y,col = rgb(.2,.2,1,.8), pch = 19)
plot(x,y,col = rgb(.2,.2,1,.3), pch = 19)
?unclass
?weekdays
.leap.seconds
weekdays(.leap.seconds)
weekdays(.leap.seconds, abbreviate)
weekdays(.leap.seconds, abbreviate = T)
weekday(.leap.seconds, abbreviate = T)
months(.leap.seconds, abbreviate = T)
quarters(.leap.seconds, abbreviate = T)
julian(.leap.seconds, abbreviate = T)
days(.leap.seconds, abbreviate = T)
dayofweek(.leap.seconds, abbreviate = T)
wday(.leap.seconds)
(z <- Sys.time())
Sys.time() - 3600             # an hour ago
z
as.POSIXlt(Sys.time(), "GMT")
as.POSIXlt(Sys.time(), wday)
as.POSIXlt(Sys.time(), 'wday')
format(.leap.seconds)
format(.leap.seconds, 'wday')
format(.leap.seconds, wday)
format(as.POSIXlt(.leap.seconds), wday)
as.POSIXlt(.leap.seconds, format = wday)
as.POSIXlt(.leap.seconds, format = yday)
as.POSIXlt(.leap.seconds, format = 'yday')
weekdays(.leap.seconds, format = 'wday')
wday(.leap.seconds)
library(lubridate)
as.POSIXlt(Sys.time())$wday
as.POSIXlt(.leap.seconds)$wday
x <- Sys.time()
p <- as.POSIXlt(x)
names(unclass(p))
p <- as.POSIXlt(Sys.time())
names(unclass(p))
p$wday
datestring <- c("January 10, 2012 10:40", "December 9, 2011 9:10")
x <- strptime(datestring, "%B %d, %Y %H:%M")
x
class(x)
datestring <- c("January 10, 2012 10:40", "December 9, 2011 09:10")
x <- strptime(datestring, "%B %d, %Y %H:%M")
x
class(x)
POSIXt
POSIXlt
x <- as.Date("2012-01-01")
y <- strptime("9 Jan 2011 11:34:21", "%d %b %Y %H:%M:%S")
x - y
POSIXlt(x) - y
as.POSIXlt(x) - y
?grep
